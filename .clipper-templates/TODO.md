# Final Steps

## Update Obsidian Web Clipper Extension

Once all template issues are fixed and tests pass:

1. Open Obsidian Web Clipper extension settings
2. Import updated templates from `templates/*.json`
3. Verify each template works on its target site
